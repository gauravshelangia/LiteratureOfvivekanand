<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?attr/backgroundColor"
    tools:context="com.vivekanand.literature.literatureofvivekanand.EnglishActivity">

    <include
        android:id="@+id/appbar"
        layout="@layout/appbar"/>

    <include
        android:id="@+id/list_of_books"
        layout="@layout/content_main"
        android:layout_below="@+id/appbar"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        />

    <include
        android:id="@+id/search_hint_view"
        layout="@layout/seach_hint_view"
        android:layout_below="@+id/appbar"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        />

    <include
        android:id="@+id/search_history_include"
        layout="@layout/search_history"
        android:layout_below="@+id/appbar"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        />

</RelativeLayout>
